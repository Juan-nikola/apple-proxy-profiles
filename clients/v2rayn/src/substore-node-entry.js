import { normalizeNodes } from "../../../shared/nodes/normalize-nodes.js";
import { filterNodesForClient } from "../../../shared/nodes/capabilities.js";
import { CLIENT } from "../../../shared/contracts.js";
import { parseV2rayNOptions } from "./options.js";
import { renderV2rayNSubscription } from "./render-node.js";
export async function operator(input, targetPlatform, context = {}) { const options = parseV2rayNOptions({ ...(context.arguments ?? {}), output: "nodes" }); if (targetPlatform !== "JSON" && options.platform !== undefined && targetPlatform !== options.platform) throw new Error(`v2rayN target platform '${targetPlatform}' does not match ${options.platform}`); if (typeof context.produceArtifact !== "function") throw new Error("v2rayN produceArtifact is unavailable"); const raw = await context.produceArtifact({ type: "collection", name: options.name, platform: "JSON", produceType: "internal" }); const normalized = normalizeNodes(raw, { clientChain: options.clientChain }); const filtered = filterNodesForClient(normalized.nodes, CLIENT.v2rayn); context.logger?.info?.(`[v2rayn-nodes] ${JSON.stringify({ accepted: filtered.nodes.length, renderFailures: filtered.diagnostics.excluded })}`); return { ...input, $content: filtered.nodes.length ? renderV2rayNSubscription({ nodes: filtered.nodes }) : `${JSON.stringify({ outbounds: [], renderFailures: filtered.diagnostics.excluded })}\n` }; }
