{
  "name": "@apple-proxy-profiles/onexray",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "build": "node --eval \"\"",
    "fixtures": "node --eval \"\"",
    "check:secrets": "node ../../scripts/check-secrets.mjs",
    "verify": "npm run test && npm run build && npm run fixtures && npm run check:secrets"
  }
}
