# Security Policy

## 支持范围

安全修复面向本仓库 `main` 分支和 GitHub Pages 的当前已发布快照。Pages 中仍保留的内容哈希快照不会原地改写，但在线窗口有界：`versions/` 最多 8 个快照，整个 `public/`（包括 `current/`、`previous/` 和 `versions/`）硬限制为 750 MiB。超出版本数或容量时会从最旧哈希快照开始清理；容量清理不会把已有窗口降到 2 个以下，若此时整棵树仍超限则发布失败。长期安全审计所需的更老版本依赖发布者事先创建的 Git tag/Release；当前工作流不会自动创建它们。

配置失效、客户端 UI 差异或规则命中异常不一定是安全漏洞。普通兼容性问题可以走公开 Issue，但必须先完成脱敏；任何可能泄露秘密、绕过 fail-closed 校验、产生意外直连或污染公开发布链路的问题，应按安全漏洞处理。

## 私密报告

不要在公开 Issue 中披露未修复漏洞或任何真实配置。请在 GitHub 仓库使用 **Security → Report a vulnerability** 创建私密安全报告：

[提交私密安全报告](https://github.com/Juan-nikola/apple-proxy-profiles/security/advisories/new)

报告应包含受影响的文件/提交、最小复现步骤、预期与实际行为、影响判断，以及不含秘密的计数或错误类别。若截图、日志或样本不是完全脱敏的，请不要上传；先用合成数据重现。

## 绝不能提交或发送的内容

以下数据不得进入 Git 历史、Pull Request、Actions 日志、公开/私密 Issue 附件、Pages、聊天、截图或复现夹具：

- 原始订阅 URL、生成后的节点/Profile URL、Sub-Store 管理地址；
- 节点主机、端口、UUID、用户名、密码、PSK、私钥、证书或认证头；
- 完整节点 URI/YAML、二维码、带秘密参数的 deep link；
- 未脱敏的 App 日志、抓包、诊断转储或真实配置备份。

即使是私密安全报告，也优先使用合成数据和聚合计数。若秘密已经出现在任何提交或外部系统中，应立即停用/轮换该秘密，并在报告中只说明已轮换的类型和暴露范围，不要再次粘贴原值。删除最新文件不足以清除 Git 历史或第三方缓存。

提交前至少运行：

```bash
npm run check:secrets
npm run verify
```

敏感信息扫描是最低门槛，不是把秘密提交到本地工作树的许可，也不能替代人工检查 `git diff --cached`。

## 威胁边界

本项目通过公开代码生成配置，但真实节点只应在用户自己的私密 Sub-Store 和设备之间流动。公开产物只能包含公开规则、来源、哈希和聚合诊断。生成器必须 fail closed：未知参数、能力降级、空节点集、输入变异或无法证明的兼容性都应停止生成，而不是静默放宽 TLS、改成直连或丢弃安全字段。

本项目明确不提供或启用：

- MITM、HTTPS 解密、CA 证书、请求重写、正文脚本或抓包；
- Sub-Store 服务器认证、TLS 终止、系统加固、备份或访问控制；
- 第三方客户端自身的漏洞修复；
- 隐藏秘密的“安全短链”；秘密 URL 不是访问控制。

如果 Sub-Store 管理页面可从公网无认证访问，应先按服务器/反向代理文档部署 TLS 和身份验证，或把它放在私有网络/VPN 后面；完成保护前不要发布任何私密节点或 Profile。

## 高风险失败条件

以下情况应停止发布或立即回滚到上一已知良好版本：

- 生成结果包含真实节点、凭据、私密 URL 或能够还原它们的诊断；
- 规则或节点解析发生未声明的静默丢弃、协议降级、TLS 放宽或意外直连；
- 公开来源无法固定到完整提交 SHA，或一次构建混用了不同上游提交；
- Manifest、文件哈希、分片计数或确定性双构建不一致；
- 真机出现 DNS 泄漏、策略目标错误、链路引用悬空，或旧配置无法恢复。

自动测试成功不等于真机安全验收。当前真机 canary 尚未完成；必须按照各客户端清单逐台测试并保留旧配置，任何失败都停止向后续设备推广。

在线回滚优先使用 `previous/` 或仍在 `versions/<manifestHash>/` 中的已验证快照。不要假设任意旧哈希会永久留在 Pages；已被保留策略移除的版本只能从可信 Git tag/Release 恢复。恢复后仍须核对 Manifest 内的文件 SHA-256，并重新执行真机 canary。

## 发布者检查

发布者应在干净依赖安装后运行完整门禁，并人工确认暂存区只包含预期公开文件：

```bash
npm ci
npm run verify
npm run check:actions
npm run check:rules
git diff --cached
```

不得通过关闭秘密扫描、放宽来源允许清单、忽略失败测试、启用 Allow Insecure 或删除回滚入口来完成发布。
